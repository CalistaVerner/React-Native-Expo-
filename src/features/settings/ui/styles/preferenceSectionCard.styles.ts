import { StyleSheet } from 'react-native';

export const preferenceSectionCardStyles = StyleSheet.create({
  card: {
    gap: 16,
  },
  fieldStack: {
    gap: 14,
  },
  fieldBlock: {
    gap: 8,
  },
  description: {
    fontSize: 13,
    lineHeight: 19,
  },
});
