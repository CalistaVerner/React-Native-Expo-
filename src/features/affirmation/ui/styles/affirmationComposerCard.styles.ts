import { StyleSheet } from 'react-native';

export const affirmationComposerCardStyles = StyleSheet.create({
  card: {
    gap: 18,
  },
  resultText: {
    fontSize: 15,
    lineHeight: 23,
  },
  promptText: {
    fontSize: 12,
    lineHeight: 18,
  },
});
