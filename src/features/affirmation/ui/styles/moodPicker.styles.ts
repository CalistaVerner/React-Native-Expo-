import { StyleSheet } from 'react-native';

export const moodPickerStyles = StyleSheet.create({
  row: {
    flexDirection: 'row',
    gap: 10,
    marginTop: 16,
  },
  emoji: {
    fontSize: 28,
    lineHeight: 32,
  },
});
